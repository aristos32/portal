import './bootstrap';

// Alpine is provided by Livewire 3 via @livewireScripts — do not import or start it here
// or you get "multiple instances of Alpine" and "Alpine.transaction is not a function"

import.meta.glob([
    '../images/**',
]);
